$(".like_<%= @travel.id %>").html("<%= j(render 'like', travel: @travel ) %>")
var user_id = $('#user_id').text();
console.log(Number(user_id))
if (Number(user_id) == Number("<%= current_user.id %>")) {
  $("#like-count").html('いいね:<%= @like.user.likes.count %>件')
}
